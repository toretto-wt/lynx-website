You can set the `fitContentWidth` and `fitContentHeight` properties for LynxView to make its size adapt to the content's width and height:

```ts
LynxView({ fitContentWidth: true, fitContentHeight: true });
```
