---
title: '正式发布 Lynxtron'
date: 2026-09-21
sidebar: false
authors: ['icecreamx10', 'GuoXi', 'lynx']
badge_text: 'Lynxtron 框架介绍'
featured: true
description: 'Lynxtron 是 Electron-like 应用框架，提供由 Lynx 驱动的轻量 UI 渲染器、跨平台 UI 模型，以及基于 UI/texture extension C-APIs 和 Node-API 的原生扩展能力。'
---

import { BlogHeader, Go } from '@lynx';

<BlogHeader authors={['icecreamx10', 'GuoXi', 'lynx']} />

Lynx 正在走向桌面端。

**[Lynxtron](/zh/lynxtron/)** 是由 Lynx 驱动的 Electron-like 应用框架。在 Lynxtron 应用中，宿主进程仍然保留桌面开发需要的 Node.js 能力，而窗口层由 `LynxWindow` 通过轻量 UI 渲染器承载 Lynx UI，而不是加载浏览器页面。

这意味着：你可以用 Lynx 的 UI 模型构建桌面应用，同时继续获得更轻的运行时、更直接的原生扩展能力，以及面向桌面、Web 和移动端的跨平台代码组织方式。

## Lynxtron 解决什么问题

桌面应用开发长期有一个现实选择：要么使用 Web 技术快速构建跨平台应用，要么投入更多原生工程成本换取体积、性能和系统能力。Lynxtron 的目标是在这两者之间提供一个新的路径：用 Lynx 组织 UI，用桌面宿主进程连接系统能力，用原生扩展承载真正需要原生实现的部分。

Lynxtron 围绕 **Light-weight and Fast**、**Multiplatform** 和 **Natively Extensible** 三项特性展开。下面观察它们如何落到桌面应用开发中。

## 使用 Lynxtron Go 体验

[Lynxtron Go](/zh/lynxtron/go) 是 Lynxtron 官方提供的 playground，它本身也是使用 Lynxtron 框架构建的桌面应用。它提供模板、内置示例 Gallery、编辑器和 Console，无需先创建完整工程，即可运行和检查小型 Lynxtron 应用。下面的可运行示例也可以直接在 Lynxtron Go 中打开。

:::info 测试版本与平台

本文示例来自 Lynxtron Go 0.1.18 release，并使用 Lynxtron runtime 0.0.28。桌面示例支持 Apple 芯片和 Intel 芯片 Mac，以及 x64 Windows；Notes 示例还支持 Web。Lynxtron 工程需要 Node.js `^22.18.0 || ^24.0.0 || ^26.0.0`。

:::

## Light-weight and Fast

Lynxtron 在应用模型上接近 Electron，但它没有把 browser 作为 UI runtime 打包进应用。[`LynxWindow`](/lynxtron/api/@lynx-js/lynxtron/Class.LynxWindow) 通过轻量化的 Lynx renderer 承载 UI，让桌面运行时更小，也让启动、窗口创建和内存使用路径更直接。

<Go
  example="benchmark"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynxtron/benchmark.png"
  deepLinkUrl="lynxtron-go://showcase/open?id=@lynxtron-examples/benchmark"
  defaultFile="src/main/desktop/main.ts"
  defaultEntryFile="dist_precompiled/desktop/main.lynx.bundle"
  defaultTab="preview"
  highlight="{17-25,39-52,66-70}"
  nativeFramework="lynxtron"
/>

> 该 benchmark 会报告 Lynxtron 的基础包体积、启动耗时、物理内存、JS heap、平台信息，以及打开第二个 `LynxWindow` 后的内存增量，覆盖一个或两个窗口时的启动和内存表现。截图记录了 Benchmark 示例 v0.0.6 使用 Lynxtron 0.0.28，在 Windows 11 x64（Core Ultra 7 155H、Intel Arc）上的一次冷启动结果（不含安装后的首次启动）；启动耗时从进程创建计至首屏内容上屏，不代表平均值。

## Natively Extensible

Lynxtron 通过 [LynxExtensionModule](/zh/api/lynx-native-api/lynx-extension-module) 扩展渲染器能力。它是由[公开的 Lynx Native API](/zh/api/lynx-native-api/lynx-view) 支撑的 Desktop C++ extension API；该 reference 覆盖这个示例使用的 native view 生命周期、注册和 texture API。原生模块使用 C++ extension module wrapper 管理接入和生命周期，并通过 [Node-API](/zh/lynxtron/Native-Libraries/NodeJS-Native-Modules) 向 JavaScript 暴露接口；原生 UI 和 texture surface 则使用 native view wrappers。在桌面开发中，一部分图形或 UI 需要原生实现是很常见的需求：摄像头预览、视频画面、地图视图、绘制画布、平台控件，或者任何需要硬件访问、GPU 深度绑定、复用系统渲染能力的 surface。Lynxtron 可以把这些原生 surface 或原生 UI 和 Lynx runtime 结合起来，让需要原生渲染的部分继续走原生路径，同时让交互和界面逻辑继续用 Lynx UI 构建和迭代。

<Go
  example="native-texture-canvas"
  deepLinkUrl="lynxtron-go://showcase/open?id=@lynxtron-examples/native-texture-canvas"
  defaultFile="native-texture-extension/module/native_texture_canvas_module.cc"
  defaultEntryFile="dist_precompiled/desktop/main.lynx.bundle"
  defaultTab="preview"
  highlight="{67-70,93-107}"
  nativeFramework="lynxtron"
/>

> Lynx UI 通过注册的原生模块控制 native texture surface，在同一组件树中组合原生渲染与前端式交互。

## Multiplatform

Lynxtron 不是把 Lynx 锁在桌面端。借助 Lynx，UI 可以运行在包括 Web 在内的多个平台上，也可以用较低成本迁移到新的宿主环境。

<Go
  example="cross-platform-notes"
  designWidth={800}
  designHeight={600}
  deepLinkUrl="lynxtron-go://showcase/open?id=@lynxtron-examples/cross-platform-notes"
  defaultFile="src/app/api.ts"
  defaultEntryFile="dist_precompiled/desktop/main.lynx.bundle"
  defaultTab="web"
  webPreviewMode="responsive"
  highlight="{71-87,146-149,165-178}"
  nativeFramework="lynxtron"
/>

> 同一套 Lynx UI 只依赖统一的 notes API。构建时，桌面和 Web 目标会分别将该 API 接入 Node preload bridge 和浏览器侧 bridge。

这对应的是 Lynxtron 的跨平台工程模型：保持 Lynx UI 层可复用，把平台相关逻辑隔离在宿主适配层中，并在需要时将渲染器模型迁移到新的宿主。

## 工程边界

对开发者来说，更重要的是可检查的源码边界：宿主进程负责桌面能力，Lynx UI 负责界面和交互，原生扩展负责需要平台能力的部分。Lynxtron 的设计重点不是把所有能力收进同一层，而是让每一层承担自己最擅长的职责。

## 开始使用

前往 [Lynxtron 快速上手](/zh/lynxtron/introduction)，创建并运行你的第一个工程。
