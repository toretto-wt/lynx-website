# merge

将传入的数据与当前数据合并。

## 语法

### Harmony

```typescript
merge(data: string | Record<string, Object> | Object): void;
```

#### 参数说明

- `data`: 字符串、键值或 json 格式的传入数据。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="lynx-native-api/template-data/merge" />