# release

释放当前 `TemplateBundle` 对象持有的 `Native` 内存。执行 `release` 方法后，`TemplateBundle` 将变为 `inValid` 状态。

## 语法

### Android

```java
public void release();
```

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="lynx-native-api/template-bundle/release" />