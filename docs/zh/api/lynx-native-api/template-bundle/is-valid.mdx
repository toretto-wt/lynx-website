---
api: lynx-native-api/template-bundle/is-valid
---

import { APISummary, APITable } from '@lynx';

# isValid

<APISummary />

确定当前的 `TemplateBundle` 对象是否有效。

## 语法

### Android

```java
public boolean isValid();
```

#### 返回值

- 如果当前的 `TemplateBundle` 对象有效，则为 `true`，否则为 `false`。

### Desktop (C++)

```cpp
bool IsValid() const;
```

#### 返回值

- 当前 `LynxTemplateBundle` 对象有效时返回 `true`，否则返回 `false`。

## 兼容性

<APITable />
