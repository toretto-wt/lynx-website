---
api: lynx-native-api/lynx-view/remove-lynx-view-client
---

import { APISummary, APITable } from '@lynx';

# removeLynxViewClient

<APISummary />

与 `addLynxViewClient` 相对应，移除 `LynxView` 生命周期事件监听。

## 语法

### Android

```java
public void removeLynxViewClient(LynxViewClient client);
```

## 参数说明

- `client`: 客户端实现的结构，并注册给 `LynxView` 实例，用于获取 `LynxView` 生命周期各流程回调。

### Desktop (C++)

```cpp
void RemoveClient(std::shared_ptr<lynx::pub::LynxViewClient> client);
```

#### 参数说明

- `client`: 此前通过 `AddClient` 注册的生命周期监听对象。传入同一个 shared client 实例会将其从 `LynxView` 生命周期监听列表中移除。

## 兼容性

<APITable />
