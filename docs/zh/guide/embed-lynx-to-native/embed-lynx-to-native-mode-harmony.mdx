### `onMeasureSize` 里得到大小约束

ArkUI 框架会在自定义组件确定尺寸时，将节点信息和尺寸范围通过 [onMeasureSize](<https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-custom-component-layout#onmeasuresize10)>) 传递给该组件。

在这个阶段可以得到外层视图给 LynxView 的大小约束，Lynx 会在系统的 `onMeasureSize` 函数内完成所有节点的排版，并计算出 LynxView 的大小。
