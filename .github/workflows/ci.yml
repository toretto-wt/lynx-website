name: CI

on:
  pull_request:
  merge_group:
    types: [checks_requested]
  push:
    branches:
      - main
      - 'release/[3-9].*'
      - 'release/[1-9][0-9].*'

concurrency:
  group: ${{ github.event_name }}-${{ github.workflow }}-${{ github.event.pull_request.number || format('{0}-{1}', github.ref, github.sha) }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout Repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: '3.13'

      - name: Setup Node and PNPM
        uses: ./.github/actions/setup-node-pnpm

      - name: Case Check
        run: node scripts/ci/check-case-collisions.mjs

      - name: Asset URL Check
        run: node scripts/ci/check-asset-urls.mjs

      - name: lynx-ui Route Contract Check
        run: node scripts/ci/check-lynx-ui-routes.mjs

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Generated Documentation Consistency Check
        run: |
          # Installation runs prepare. Keep this list aligned with every
          # committed documentation output generated during that lifecycle.
          generated_documentation_outputs=(
            "sharedDocs/packageDocs"
            "sharedDocs/lynx-ui-intros.ts"
          )
          changes="$(git status --porcelain=v1 --untracked-files=all -- "${generated_documentation_outputs[@]}")"
          if [ -n "$changes" ]; then
            printf '%s\n' "$changes"
            echo "::error::Generated documentation is out of date. Run pnpm run prepare and commit all changes reported above."
            exit 1
          fi

      - name: Install pipx
        run: python -m pip install pipx==1.17.1

      - name: Living Spec Check
        run: |
          # The generated HTML is committed; fail when source and output drift.
          pnpm run gen:living-spec
          git diff --exit-code -- docs/public/living-spec/index.html

      - name: Format Check
        run: pnpm run format:check

      - name: Lynx Compat Data Test
        run: pnpm --filter @lynx-js/lynx-compat-data run test

      - name: Lynx Compat Data Pack Check
        run: pnpm --filter @lynx-js/lynx-compat-data run pack:check

      - name: Build
        run: pnpm run build

  # Validate published core example metadata separately from site generation.
  # This check needs dependencies but does not need root prepare lifecycle work.
  dependency-provenance:
    name: Dependency Provenance
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout Repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Node and PNPM
        uses: ./.github/actions/setup-node-pnpm

      - name: Install Dependencies Without Lifecycle Scripts
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Example Package Provenance Check
        run: |
          node --test scripts/ci/check-example-package-provenance.test.mjs
          node scripts/ci/check-example-package-provenance.mjs

  # Keep generator portability tests dependency-free. Direct Node execution
  # avoids pnpm 11 implicitly installing dependencies and running root prepare.
  generator-portability:
    name: Generator Portability
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout Repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: '.node-version'

      - name: Lynx Example Generator Test
        run: node --test scripts/lynx-example.test.js

  # Check committed documents with MDX dependencies, without running prepare.
  # Validate separately verifies generated documentation after root prepare.
  # Keep installation explicit and separate from dependency-free generator tests.
  documentation-imports:
    name: Documentation Imports
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout Repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Node and PNPM
        uses: ./.github/actions/setup-node-pnpm

      - name: Install Dependencies Without Lifecycle Scripts
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Documentation Import Boundary Check
        run: |
          node --test scripts/ci/check-doc-import-boundaries.test.mjs
          node scripts/ci/check-doc-import-boundaries.mjs

  # Enforce final media blobs relative to the target tree of the synthetic merge.
  # The offline media processor is deliberately not invoked by CI.
  media-budget:
    name: Media Budget
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout Repo
        if: ${{ github.event_name == 'pull_request' || github.event_name == 'merge_group' }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 2

      - name: Setup Node.js
        if: ${{ github.event_name == 'pull_request' || github.event_name == 'merge_group' }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: '.node-version'

      - name: Test Media Budget
        if: ${{ github.event_name == 'pull_request' || github.event_name == 'merge_group' }}
        run: node --test scripts/media/check-budget.test.mjs

      - name: Check Media Budget
        if: ${{ github.event_name == 'pull_request' || github.event_name == 'merge_group' }}
        run: node scripts/media/check-budget.mjs HEAD^1 HEAD

      - name: Skip Push Check
        if: ${{ github.event_name == 'push' }}
        run: echo "[media-budget] success — media budget is enforced before merge."

  # Branch protection requires Done as the aggregate status. Add every new
  # required CI job to needs so its result cannot bypass this gate.
  done:
    name: Done
    needs:
      - validate
      - dependency-provenance
      - generator-portability
      - documentation-imports
      - media-budget
    if: ${{ always() }}
    runs-on: ubuntu-latest
    permissions: {}

    steps:
      - name: Fail If Any Job Did Not Succeed
        if: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'skipped') || contains(needs.*.result, 'cancelled') }}
        run: exit 1
